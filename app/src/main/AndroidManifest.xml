<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <!-- Desde API 30 una app solo ve los paquetes "visibles"; sin esto la
         lista de PantallaDeAjustes queda practicamente vacia (solo Cerrojo) y
         no se puede elegir ninguna app que vigilar. Sideloaded, sin politica
         de Play que lo prohiba, y necesario tambien para que UsageStatsManager
         no filtre por visibilidad en el camino de deteccion de primer plano. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:label="Cerrojo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cerrojo">
        <service
            android:name=".servicio.ServicioDeVigilancia"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Vigila el tiempo de uso de otras apps para bloquearlas" />
        </service>
        <receiver android:name=".servicio.ReceptorDeArranque" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <activity
            android:name=".ui.PantallaDeBloqueo"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=".bloqueo"
            android:theme="@style/Theme.Cerrojo" />
        <activity android:name=".ui.Principal" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.Shell" android:exported="false" />
    </application>
</manifest>
